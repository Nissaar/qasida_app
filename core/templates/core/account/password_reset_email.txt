Someone asked to reset the password for the Qasida Library account "{{ user.get_username }}".

If that was you, follow this link to choose a new one:

{{ protocol }}://{{ domain }}{% url 'password_reset_confirm' uidb64=uid token=token %}

The link works once, and only for a few hours.

If it was not you, nothing has changed and you can ignore this message.

-- Qasida Library
